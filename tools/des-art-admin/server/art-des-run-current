#!/usr/bin/env bash
set -euo pipefail
cd /var/www/art-des/current
if [[ -f server.js && -d .next ]]; then exec /usr/bin/env HOSTNAME=127.0.0.1 PORT=3000 NODE_ENV=production /usr/bin/node server.js; fi
exec /usr/bin/npm run start -- --hostname 127.0.0.1 --port 3000
