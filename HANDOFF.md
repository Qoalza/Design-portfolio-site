# HANDOFF

Обновлено: 2026-08-12

## Назначение

Это живой источник истины для Codex по текущему состоянию проекта. Он хранит решения, прогресс, блокеры и следующий проверяемый шаг, но не заменяет `AGENTS.md`.

Перед началом работы:

1. прочитать `AGENTS.md` и этот файл;
2. проверить, что текущая ветка, commit и фактическое состояние проекта совпадают с описанием ниже;
3. при расхождении доверять репозиторию и обновить этот файл;
4. после каждого завершённого этапа обновлять статусы, доказательства проверок и следующий шаг.

Никогда не записывать сюда пароли, приватные ключи, токены или другие секреты.

## Граница рабочих контекстов

- Обсуждение: архитектура, планирование, риски, вспомогательная документация и handoff-артефакты.
- Work local: исходники, зависимости, команды, Git, подключение к VPS, серверная конфигурация и публикация.

## Цель

Опубликовать на `https://art-des.ru` сайт-портфолио Product Designer с полным видимым соответствием утверждённому макету Figma в desktop-диапазоне.

## Текущий релиз

Первый срочный этап главной завершён и находится на пользовательской проверке. Следующий текущий этап — первая открытая страница проекта Corvo и переиспользуемый скелет для будущих проектных кейсов.

Источник дизайна:

- Figma: <https://www.figma.com/design/5ZzspE0OrqesDcTP0RRPHr/%D0%9A%D0%BE%D0%BD%D1%86%D0%B5%D0%BF%D1%82?node-id=262-2382>
- File key: `5ZzspE0OrqesDcTP0RRPHr`
- Node: `262:2382`
- Имя фрейма: `Test design`
- Размер Figma-фрейма для контрольного снимка: `1440 × 5976 px`
- Рабочая область контента: `1200 px`, всегда по центру viewport
- Внешняя область страницы: резиновая и заполняет доступную ширину
- Приоритет: визуальное соответствие макету, без самостоятельного изменения дизайна.

Источник дизайна открытого проекта Corvo:

- Figma: <https://www.figma.com/design/5ZzspE0OrqesDcTP0RRPHr/%D0%9A%D0%BE%D0%BD%D1%86%D0%B5%D0%BF%D1%82?node-id=321-29865>
- File key: `5ZzspE0OrqesDcTP0RRPHr`
- Node: `321:29865`
- Имя фрейма: `Test open project`
- Контрольный размер: `1440 × 3917 px`
- Контентный контейнер: максимум `1200 px`, центрирован; внешние поля фрейма — по `120 px`.
- Этот фрейм является источником истины для визуала и контента Corvo и временным скелетом будущих открытых проектов.
- Количество секций, списков и изображений в других проектах может отличаться; реализация не должна жёстко фиксировать структуру только под Corvo.

Функции первого релиза:

- «Связаться» открывает личный чат Telegram;
- «CV» скачивает финальное резюме в PDF;
- остальные взаимодействия реализуются только при наличии однозначного поведения в макете.

Не входит в первый релиз:

- каталог проектов;
- CMS или админка;
- бэкенд, база данных и авторизация;
- адаптив для viewport уже `1280 px` и придумывание отсутствующих адаптивных макетов;
- полноценная дизайн-система в Figma или коде.

## Критерий визуальной готовности

- Контентная область имеет ширину `1200 px` и центрируется на desktop viewport.
- При viewport `1440 px` ожидаемые внешние поля составляют по `120 px` с каждой стороны.
- На более широком экране контент остаётся шириной `1200 px`, а внешние поля растут симметрично.
- Figma-фрейм `1440 × 5976 px` используется как контрольный снимок, а не как фиксированная ширина всей страницы.
- Реализация и экспорт Figma сравниваются при viewport `1440 px`, желательно наложением.
- Исправляются видимые расхождения сетки, размеров, отступов, типографики, границ, радиусов, цветов, изображений и переносов.
- Незначительные различия сглаживания текста между ОС не считаются исправимым дефектом.
- Viewport уже `1280 px` относится к будущему адаптиву и не входит в текущий этап. Визуальные решения для него не придумывать.

## Дизайн-контекст и риски

- В Figma пока нет оформленных токенов, текстовых стилей и полноценной компонентной системы.
- В макете используются `Google Sans`, `Onest` и `Source Code Pro`; все три семейства берутся из Google Fonts.
- В макете много отдельных SVG и растровых изображений. Временные ссылки Figma нельзя оставлять в production; оригиналы нужно сохранить в репозитории.
- Изменение эталонного фрейма во время визуальной QA требует повторной фиксации эталона.

Передача ассетов:

- если оригиналы доступны внутри Figma, Codex экспортирует и сохраняет их самостоятельно;
- если исходников нет в Figma, допустим один ZIP с сохранённой структурой папок и понятными именами;
- иконки, логотипы и векторная графика передаются оригинальными файлами `.svg`;
- растровые изображения передаются оригинальными `.png`, `.jpg` или `.webp` без повторного сжатия и без вставки в общий скриншот;
- не конвертировать SVG в PNG и не собирать отдельные изображения в один лист.

## Архитектура

Текущее подтверждённое состояние репозитория:

- Next.js с App Router;
- TypeScript;
- npm;
- MDX и `gray-matter` для будущего контента проектов;
- маршруты `/projects` и `/projects/[slug]` уже существуют;
- без Tailwind CSS, сторонних UI-библиотек, CMS, базы данных и авторизации.

Решения:

- бэкенд для текущего релиза не нужен;
- будущие проекты сначала хранятся в MDX;
- открытая страница проекта должна использовать общий переиспользуемый shell и MDX-компоненты; порядок и количество контентных блоков задаются конкретным `.mdx`, а не жёстко прошиваются в route-компоненте;
- редактирование через админку отложено;
- код может иметь минимальные внутренние CSS-переменные и повторно используемые компоненты, даже если в Figma они ещё не систематизированы;
- будущая CMS должна подключаться через заменяемый слой данных, если она действительно понадобится.

## Навыки Codex и порядок применения

Установлены глобально в Codex:

- `vercel-react-best-practices` — применять во время написания и ревью React/Next.js кода. Выбирать только релевантные правила для статичной главной: размер клиентского кода, изображения, шрифты и эффективность рендера. Не добавлять архитектуру и зависимости без необходимости.
- `accessibility` — применять во время семантической верстки ссылок, кнопок, изображений и навигации, затем провести отдельную проверку перед Pull Request. Если требование WCAG конфликтует с утверждённым макетом, не останавливать работу и не запрашивать решение: зафиксировать расхождение, реализовать вариант из Figma и включить исключение в итоговый отчёт. Не заявлять о полном соответствии WCAG при наличии таких исключений.
- `web-quality-audit` — применять только после достижения визуального соответствия и успешной локальной сборки. Сначала сформировать read-only отчёт по Performance, Accessibility, SEO и Best Practices, затем исправлять только относящиеся к текущему релизу Critical/High проблемы. Замечания по адаптиву меньше `1280 px` пока не блокируют релиз.
- `typography` — обязательно применять перед изменением шрифтов, весов, tracking, kerning, line-height, OpenType-настроек и стратегии загрузки web fonts, а также повторно перед пользовательской визуальной приёмкой типографики. Сначала провести аудит фактических Figma-стилей, подключённых файлов, variable-осей, computed styles и реально загруженных начертаний; только затем менять CSS.

Правила применения `typography`:

1. Figma и явно сформулированная пользователем визуальная цель имеют приоритет над универсальными рекомендациями скилла.
2. Не заменять Google Sans, Onest и Source Code Pro и не перестраивать type scale по общим формулам без прямого запроса.
3. Для variable fonts разрешены промежуточные веса, включая `450` и `550`, если они поддерживаются файлом и дают требуемую иерархию.
4. Проверить диапазоны weight axes, корректность `@font-face`, отсутствие синтетического bold/italic, kerning, font loading, fallback и FOUT/CLS.
5. Инвентаризировать типографические роли страницы и устранять случайный разброс весов и letter-spacing по ролям, а не независимыми точечными правками.
6. Сравнивать типографику с Figma при масштабе браузера `100%`, одинаковом viewport и после подтверждённой загрузки web fonts.
7. Общий интернет-ресерч и заимствование правил «крупных компаний» не выполнять по умолчанию. Обращаться к актуальным первичным источникам только для конкретного технического вопроса, которого нет в скилле или документации используемого шрифта/браузера.
8. Если рекомендация по читаемости визуально конфликтует с Figma, действует политика конфликтов Figma и WCAG ниже.
9. После изменений Codex может поставить только `READY_FOR_USER_REVIEW`; окончательную типографическую приёмку выполняет пользователь.

Уже доступные Figma/Product Design навыки остаются источником дизайн-контекста и визуальной QA. Новые навыки не могут переопределять Figma и не дают разрешения придумывать визуальные решения.

## Политика конфликтов Figma и WCAG

- Визуальное исполнение утверждённого макета Figma имеет приоритет над рекомендациями и критериями WCAG.
- Если доступное исполнение возможно без видимого отклонения от макета, применять его без отдельного согласования.
- Если соблюдение WCAG требует изменить цвет, контраст, размер, композицию, видимое состояние или иное утверждённое решение, реализовать Figma без запроса пользователю.
- Каждый подтверждённый конфликт записывать в этот файл и в `ACCESSIBILITY_EXCEPTIONS.md` с указанием места, критерия WCAG, требуемого доступного варианта, фактического решения по Figma, влияния и ссылки на Figma node при наличии.
- Наличие исключения не блокирует Pull Request и текущий релиз, но исключение обязательно показывается пользователю в итоговом отчёте.

## Инфраструктура

Подтверждено:

- домен: `art-des.ru`;
- `art-des.ru` и `www.art-des.ru` имеют A-записи на `185.219.41.147` в панели DNS;
- VPS: Ubuntu 24.04;
- ресурсы: 1 vCPU, 2 ГБ RAM, NVMe 30 ГБ;
- на VPS нет других сайтов;
- пароль первого входа доступен в панели сервера, но не должен передаваться в чат или сохраняться в Git.

Требует read-only проверки на VPS:

- фактический SSH-логин, предположительно `root`;
- доступность SSH и состояние портов;
- наличие Node.js, npm, Git, Nginx, systemd и firewall;
- наличие и состояние SSL;
- свободное место и память;
- необходимость swap.

Предпочтительный минимальный deployment-стек, пока не установлен и не подтверждён:

- Node.js и npm;
- Git;
- Nginx как reverse proxy;
- systemd для процесса Next.js;
- Let's Encrypt для SSL;
- без Docker и PM2, если read-only аудит не выявит иной необходимости.

## Git и подтверждённое состояние

Текущее подтверждённое локальное состояние:

- Git-хостинг проекта: GitHub, не GitLab;
- remote `origin`: `https://github.com/Qoalza/Design-portfolio-site.git`;
- базовая ветка: `main`;
- подтверждённый базовый commit: `2e2bbe4`;
- `main` совпадала с `origin/main`;
- исходный commit ветки: `2e2bbe4`;
- рабочая ветка реализации: `feature/homepage-figma`;
- текущая рабочая ветка кейса: `feature/project-corvo`, создана от локального homepage HEAD `69dbdbb24c29716e6ff3aad9b45efc959b66a41f` с сохранением всех незакоммиченных изменений;
- исходные незакоммиченные документы `AGENTS.md`, `HANDOFF.md` и `ACCESSIBILITY_EXCEPTIONS.md` сохранены и включены в рабочий объём;
- эталон Figma и все используемые production-ассеты сохранены локально, временных Figma URL в коде нет.

Перед началом реализации повторно проверить эти сведения. Реализацию вести в отдельной feature-ветке, затем выполнить локальную визуальную QA, push и создать Pull Request в `main`. Pull Request является точкой контроля результата; merge и публикацию на VPS не выполнять без явного разрешения пользователя.

## План исполнения

Статусы: `not_started`, `in_progress`, `blocked`, `done`.

1. `done` — создать и утвердить `HANDOFF.md` как источник истины.
2. `in_progress` — Telegram URL и CV PDF ожидаются; это не блокирует Pull Request. Для обоих подготовлены видимые точки подключения без выдуманных URL.
3. `done` — проведён read-only аудит локального проекта и Git. VPS не затрагивался: текущая цель прямо исключает публикацию и серверные изменения.
4. `done` — эталон Figma `1440 × 5976 px`, исходные изображения, SVG и точные начертания Google Sans, Onest и Source Code Pro сохранены локально.
5. `done` — главная реализована проверяемыми секциями без новых зависимостей и без изменения существующих контентных маршрутов.
6. `done` — после отклонения прежней версии проведена полная инвентаризация, восстановлены состав, структура, контент и ассеты, созданы новые посекционные comparison 1:1. Все секции имеют статус `READY_FOR_USER_REVIEW`; старый comparison остаётся недействительным.
7. `done` — повторно пройдены lint/build, production route smoke, DOM/accessibility, web-quality и React/code review. Новых Critical/High дефектов не найдено; Figma/WCAG исключения записаны отдельно.
8. `not_started` — подготовить VPS и SSL после отдельного разрешения на изменение сервера.
9. `not_started` — развернуть проверенный commit после одобрения merge/deploy.
10. `not_started` — выполнить production smoke-check и записать URL, commit и результат сюда.
11. `done` — реализовать `/projects/corvo` и переиспользуемый MDX project shell, провести Figma comparison, accessibility/web-quality/code checks, lint, build и production route smoke.
12. `done` — синхронизировать Corvo со свежим Figma node `321:29865`: исправить neutral header home, breadcrumbs, back, platforms, заменить process asset и добавить общий доступный lightbox для `ProjectMedia`.
13. `done` — централизовать scroll policy: новый pathname без hash открывается с `scrollY = 0`, browser Back/Forward используют manual restoration, а явные hash-ссылки сохраняют якорную навигацию и `scroll-margin`.

## Стоп-линии

Остановиться и запросить решение, если:

- отсутствующее состояние Figma требует придумать дизайн;
- без точного шрифта предлагается утверждать pixel-perfect;
- эталонный Figma-фрейм изменился во время реализации;
- обнаружены неожиданные сервисы или данные на VPS;
- действие затрагивает production, DNS, SSL, firewall или доступы без явного разрешения;
- требуется сохранить секрет в репозитории или переписке;
- сборка или обязательные проверки не проходят;
- требуется merge в `main` без разрешения пользователя.

Конфликт Figma с WCAG не является стоп-линией: действует политика исключений выше.

## Необходимые входные данные

- Telegram URL: `pending`.
- Финальный CV PDF: `pending`.
- Точные начертания и веса шрифтов извлечены из Figma и получены из Google Fonts: `done` (`400`, `500`).
- Подтверждённый SSH-логин: `pending`.
- Результат read-only аудита VPS: `pending`.
- Решение о временном поведении навигации на ещё не созданные страницы: реализовано как нефокусируемые недоступные пункты по Figma; новые маршруты не создавались.

## Definition of Done первого релиза

- Главная не имеет видимых расхождений с Figma при контрольном viewport `1440 px`; контентная область `1200 px` центрирована и сохраняет ширину на более широких desktop viewport.
- Все production-ассеты локальны и оптимизированы достаточно для нормальной загрузки.
- Telegram-ссылка и скачивание CV работают.
- `npm run lint` и `npm run build` проходят.
- Все выявленные конфликты Figma с WCAG перечислены в `ACCESSIBILITY_EXCEPTIONS.md`; при наличии исключений результат не описывается как полностью соответствующий WCAG.
- Сайт доступен по HTTPS на основном домене; поведение `www` зафиксировано.
- Production smoke-check выполнен с внешней стороны.
- В этом файле записаны итоговый commit, выполненные проверки, известные ограничения и следующий шаг.

## Последнее подтверждённое действие

Глобальное поведение прокрутки исправлено на уровне root layout. Причина дефекта — сочетание `html { scroll-behavior: smooth }`, отсутствия `data-scroll-behavior="smooth"` для штатного Next.js route-scroll и браузерного `history.scrollRestoration = "auto"`, которое при App Router history traversal восстанавливало сохранённую позицию другого маршрута. `NavigationScrollController` выставляет manual restoration и синхронно сбрасывает новый pathname без hash в `0` с временным `scroll-behavior: auto`; hash-переходы остаются под штатной якорной навигацией и сохраняют `scroll-margin`.

В dev и production проверены: главная → Corvo, back-кнопка, breadcrumb, browser Back/Forward, прямое открытие Corvo, `#projects` (`48 px` от верха), lightbox без маршрутного reset. Все обычные межстраничные переходы завершились с `scrollY = 0`, console errors/hydration warnings отсутствуют. `npm run lint` и `npm run build` прошли. Commit реализации: `3d313c0`. Merge, deploy, VPS, DNS и production не затрагивались.

По повторно полученному 2026-08-12 `get_design_context` Figma node `321:29865` страница Corvo обновлена в существующей ветке и PR #9. «Главная» в project header имеет нейтральное состояние без `aria-current`; breadcrumbs используют Google Sans `16/20`; back — `44 × 40 px` без border; Desktop/Mobile используют свежие SVG `20 × 20 px` и вертикальный separator `#e1e8ed`. Production `process-flow.png` заменён свежим оригиналом Figma `1960 × 546`.

Добавлен переиспользуемый клиентский lightbox для всех `ProjectMedia`: четыре изображения Corvo открываются, сохраняют aspect ratio и quality, overlay закрывается `Escape` и кликом по свободному фону, клик по изображению не закрывает его, background scroll блокируется без скачка, scroll position и focus возвращаются. Семантика: `role="dialog"`, `aria-modal="true"`, доступное имя. Содержимое кейса, порядок секций, главная и адаптив ниже `1280 px` не изменялись.

Свежий Figma export, browser capture, full/focus comparisons и два открытых lightbox находятся в `design-reference/corvo-fresh/`; `design-qa.md` имеет `final result: passed`, а внешний статус остаётся `READY_FOR_USER_REVIEW`. `npm run lint`, `npm run build` и production route smoke пройдены; browser console ошибок и hydration warnings не содержит. Commit исправлений: `ae73278`. Merge, deploy, VPS, DNS и production не затрагивались.

По свежему Figma node `262:2382` выполнена точечная доработка текущей реализации в PR #8:

- проверены типографические роли Google Sans, Onest и Source Code Pro; `@font-face` описывает variable-диапазон `400 500`, синтетические начертания отключены, роли Freelance используют точный промежуточный вес `450`;
- button/CTA-иконки заменены с CSS mask на реальные SVG `16 × 16 px` со stroke `1 px` и цветами из Figma;
- все три линии процесса воспроизводят отдельные neutral/colored сегменты, dash `8/8`, отступы и длины `442/180/72 px`;
- AI-разделитель имеет высоту `252 px` внутри grid `348 px`, отступы `48 px`, цвет `#ecedee` и толщину `1 px`;
- переход «Мои работы» проверен реальным кликом: `#projects` останавливается на `48 px` от верхнего края viewport;
- знак copyright в footer приведён к цвету соседнего текста `#8c949b`;
- заголовок изменён на «Резюме», строка Freelance дословно синхронизирована со свежим Figma.

Контроль: viewport `1440 px`, zoom `100%`, DOM `1440 × 5976 px`, web fonts имеют статус `loaded`, горизонтального переполнения нет. Новых accessibility-исключений нет; применимы уже записанные исключения контраста и desktop-only reflow. `npm run lint` и `npm run build` прошли, локальная smoke-проверка главной и затронутых областей пройдена. Commit реализации: `88c371779be7435ca8c2c663c0f1d0f6c9f5d727`. Статус — только `READY_FOR_USER_REVIEW`; `PASSED` не присваивался. Merge, deploy, DNS и VPS не затрагивались.

Исходный `content/projects/corvo.mdx` расширен актуальными typed frontmatter-метаданными и четырьмя локальными production-изображениями; MDX компилируется и полностью отображается через общий project shell. Период `2026`, частичная публичность и NDA-note согласованы с утверждённым Figma-фреймом.

Страница `/projects/corvo` реализована по Figma node `321:29865` на ветке `feature/project-corvo`. Общий route shell читает типизированные optional frontmatter-поля, поддерживает произвольное количество MDX-секций, заголовки, списки, выделение, dividers, изображения и подписи. Локально сохранены точные Corvo logo/icons и четыре production-изображения. Карточка Corvo на главной ведёт на `/projects/corvo`; Figma action использует URL из frontmatter; Share использует Web Share API с copy fallback.

Контроль: viewport `1440 × 900`, `deviceScaleFactor: 1`, full page `1440 × 4237`, fonts `loaded`, горизонтального переполнения нет. Эталон, реализация, полный и три фокусных comparison находятся в `design-reference/corvo/`; `design-qa.md` имеет `final result: passed`. `npm run lint`, `npm run build` и production route smoke прошли. Новых Critical/High web-quality или accessibility-дефектов нет; отдельное Figma/WCAG-исключение малых метаданных записано в `ACCESSIBILITY_EXCEPTIONS.md`. Commit реализации: `166ba94`. Merge, deploy, DNS и VPS не затрагивались.

## Следующий шаг

Открыть свежий `/projects/corvo?review=<final-sha>` из финального HEAD и передать результат пользователю. PR #9 обновлён. Merge, deploy и VPS не затрагивать без отдельного разрешения пользователя.
